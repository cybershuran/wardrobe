# 我的衣橱 · 使用说明

一个仿 [tandpfun/wardrobe](https://github.com/tandpfun/wardrobe) 的精简版。单个网页文件，双击就能用，不用装任何东西、不用写代码。

## 怎么用

1. **打开** `我的衣橱.html`（双击，用浏览器打开，推荐 Chrome / Edge）。
2. **填 Key**：在「① 设置」里粘贴你的 OpenAI API Key，点「保存设置」。
   - Key 在这里申请：https://platform.openai.com/api-keys
   - Key 只存在你自己电脑的浏览器里，不会上传到别处。
3. **传照片**：在「② 上传照片」拖入或点击选一张穿搭照，点「识别单品」。
   - AI 会列出照片里每件衣服（名称、品类、颜色、材质、季节）。
4. **入库**：点每件下面的「加入衣橱」，它就进了「③ 我的衣橱」，会一直存着。
5. **生成单品图**：在衣橱里点某件的「生成单品图」，AI 画一张干净的白底商品图（模拟原项目的抠图效果）。
6. **搭配**：在「④ AI 搭配建议」填个场合（可留空），点「帮我搭配」，AI 从你衣橱里挑 2-3 套。

## 花多少钱

- 识别用的是便宜的视觉模型（默认 `gpt-4o-mini`），一张照片大约几分钱。
- 「生成单品图」用的是图像模型（默认 `gpt-image-1`），每张贵一些（约一两毛到几毛），按需点就行。
- 具体价格以 OpenAI 官网为准。模型名如果哪天不可用，直接在「① 设置」里把模型名改掉即可。

## 和原项目的区别

原项目是完整的开发工程（要装 Node、跑命令、有本地服务器和文件数据库）。这个版本把核心体验压缩成一个网页文件，方便不写代码的人直接上手。核心流程一样：**识别 → 抠图 → 入库 → 搭配**。

想进一步升级（比如更精准的抠图、导出衣橱、多人共享）随时找我。
